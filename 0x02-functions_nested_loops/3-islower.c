# include "main.h"
/**
 * _islower - is a function
 * @c: is a variable that is input to the function
 * Description: it checks if an alphabet is lowercase
 * Return: int value or 0 or 1
 */
int _islower(int c)
{
if (c < 97)
{
return (0);
}
else
{
return (1);
}
}
